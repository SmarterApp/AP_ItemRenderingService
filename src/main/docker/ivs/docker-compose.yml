version: '2.1'
services:
    ivs-service:
        container_name: ivs-service
        image: osucass/ap_itemviewerservice:3.3.2.RELEASE
        expose:
            - 8080
        ports:
            - 8080:8080
        networks:
            - spring-cloud-network
        volumes:
            - ${HOME}/ItemBankIVS/:/home/tomcat7/content/
        logging:
            driver: json-file
networks:
    spring-cloud-network:
        driver: bridge